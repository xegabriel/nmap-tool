# nmap-tool
REST API that processes, stores, and serves nmap results

# How to run
```shell
brew install nmap
nmap --version
cd ../nmap-tool
docker-compose up -d
# To stop it, run:
docker-compose down
```